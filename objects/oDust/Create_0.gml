/// @description Instance create
// Defining variables
image_speed = random_range(0.5, 1); // randomises how the dust animates
image_index = random_range(0, 6); // randomises the start frame
hsp = random_range(-2, 2); 
vsp = random_range(-2, 2);
image_xscale = choose(1, -1); 
image_yscale = choose(1, -1);
